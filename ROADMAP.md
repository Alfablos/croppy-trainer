# Roadmap

## Performance

1. ~~Add .to_type_cpu() and .to_type_gpu() to Precision returning types (E.g. `np.float32()` depending on precision and device)~~
2. Combining CPU + GPU
3. Add a precision selector (FP32, FP16, UINT8)
4. Federate with a cloud instance if it makes sense.


